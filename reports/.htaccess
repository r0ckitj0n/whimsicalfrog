# Block direct public access to reports/ (Apache 2.4+)
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>

# Fallback for older Apache versions
<IfModule !mod_authz_core.c>
  Deny from all
</IfModule>
