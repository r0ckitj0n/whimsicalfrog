# Lines are extended regular expressions matched against the file path prefix before colon
# Use anchors like ^\./ to match from repository root

# Allow all maintenance/CLI scripts under scripts/
^\./scripts/.*\.php$

# Allow PHP dev-test utilities
^\./scripts/php/dev-tests/.*$

# Allow local DB connection test
^\./db_connection_test\.php$
